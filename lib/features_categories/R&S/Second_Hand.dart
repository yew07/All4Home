import 'package:flutter/material.dart';

class Second_Hand extends StatelessWidget {
  const Second_Hand({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Title(
        color: Colors.black,
        child: Text("Second Hand Market"),
      ),
    );
  }
}
