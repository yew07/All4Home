import 'package:flutter/material.dart';

class BestBuy extends StatelessWidget {
  const BestBuy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Best Buy"),
      ),
    );
  }
}
